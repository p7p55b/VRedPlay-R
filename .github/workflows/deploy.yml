name: Déploiement Serveur Local (192.168.4.26)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Déploiement et redémarrage VRedPlay
    runs-on: self-hosted

    steps:
      - name: Exécution du script de déploiement sécurisé
        run: /usr/share/nginx/html/deploy.sh
